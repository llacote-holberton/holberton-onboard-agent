# HOW TO USE: please "save as" with filename '.env' in same folder
#   then adjust the values (after the =) as needed.

# Project name (will prefix all container names)
COMPOSE_PROJECT_NAME=hbn-onboarder

# = Ollama Local model handler configuration =
OLLAMA_HOST=ollama
OLLAMA_PORT=11434
OLLAMA_MODEL=qwen3:8b

# Timeout (seconds) for a SINGLE agent -> Ollama call, during plan
# generation. This is the innermost layer of a coordinated timeout chain
# (frontend -> backend -> agent -> Ollama): backend and frontend read
# this SAME value and each add their own margin on top of it in code
# (+15s, +30s respectively -- see agent/planner.py::_OLLAMA_CALL_TIMEOUT
# for the full rationale), so you only need to change this ONE number.
# Raise it if you're on a slower machine or a larger model tag -- e.g.
# a bigger qwen3 tag is noticeably slower per call than the default,
# especially on a cold start.
OLLAMA_CALL_TIMEOUT_SECONDS=110

# = MCP Server configuration =
HBN_MCP_PORT=8200

# = Mailhog (fake SMTP + web UI, used by the mcp-server "mailbox" tool) =
MAILHOG_SMTP_PORT=1025
MAILHOG_WEB_PORT=8025
# Once running: open http://localhost:8025 to see emails the mailbox tool sent.

# = AI "Helper" Agent configuration =
AGENT_PORT=8100

# Backoffice configuration
BACKEND_PORT=8000

# Configuration for persistent data
DATA_DIR=/app/data
DATA_HOST_DIR=./data

# Frontend configuration
FRONTEND_PORT=8501

# GitHub configuration for issues
GITHUB_TOKEN=<please-generate-your-token-following-instructions-from-the-provider>
GITHUB_REPO=llacote-holberton/holberton-onboard-agent

