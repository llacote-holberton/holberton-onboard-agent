name: ${COMPOSE_PROJECT_NAME}

services:

  # Local LLM handler, for now only option to use application so always started
  ollama:
    image: ollama/ollama:latest
    container_name: ${COMPOSE_PROJECT_NAME}-ollama
    ports:
      - "${OLLAMA_PORT:-11434}:${OLLAMA_PORT:-11434}"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

  # Service providing curated access to filesystem/db/APIs for the agent
  mcp-server:
      build:
        context: ${PWD}/mcp_server
        dockerfile: DockerFile
      ports:
        # Warning: right side port must match port(s) exposed in DockerFile
        - "${HBN_MCP_PORT:-8200}:${HBN_MCP_PORT:-8200}"
      environment:
        - HBN_MCP_PORT=${HBN_MCP_PORT:-8200}
      restart: unless-stopped

  # Agent : dual-step LLM: planner (calls Ollama) + executor (client MCP).
  # Receives user intent, constructs plan, waits approval from API,
  #   then executes only what has been validated.
  agent:
    build: ${PWD}/agent
    ports:
      - "${AGENT_PORT:-8100}:${AGENT_PORT:-8100}"
    depends_on:
      - ollama
      - mcp-server
    environment:
      - OLLAMA_HOST=${OLLAMA_HOST:-ollama}
      - OLLAMA_PORT=${OLLAMA_PORT:-11434}
      - OLLAMA_API_BASE=http://${OLLAMA_HOST:-ollama}:${OLLAMA_PORT:-11434}
      - OLLAMA_MODEL=qwen3:8b
      - MCP_SERVER_URL=http://mcp-server:${HBN_MCP_PORT}
      # Used to tell Python to not output Warning level (only) messages
      # Useful because Google ADK will otherwise output several warnings during use
      # One at start because cannot connect to Google Cloud since we didn't configure
      #   "Google Application Default Credentials" so warns it fallbacks on "local".
      # Others during run because it has features still in "experimental stage".
      - PYTHONWARNINGS=ignore
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/data


volumes:
  ollama_data:
